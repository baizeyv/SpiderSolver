# CXX Spider Solver

用于尝试解决 `PlayValve` 和 `Vita` 的蜘蛛纸牌关卡的CLI小工具.

## Mode

### TestMode
测试模式

### DebugMode
Debug关卡模式

### BatchMode
批量模式

## TODO
关卡生成模式,目前已经可以生成了,但是需要一份 类`PlayValve` `seed` 的文本文件,通过调用PlayValve的BatchMode来实现,但为了方便还是再写一个GenMode更好.
